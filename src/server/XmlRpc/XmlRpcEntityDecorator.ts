import 'reflect-metadata';
import { XmlRpcEntity } from './XmlRpcEntity';

// tslint:disable-next-line: ban-types
export const XmlRpcEntityDecorator = (constructorFunction: any) => {
  // const t = new constructorFunction();
  // t.getKeys().forEach((x) => {
  //   // t[x] = undefined || null || 1 || '';
  // });
  // return t;
};
